import { createApp } from "../../server/_core/app";

/**
 * Source handler for the Vercel tRPC Function. Vercel deploys the bundled
 * catch-all artifact generated from this module by vercel.json.
 */
export default createApp();
